import { WordCounter } from '../wordCounter.js';

describe('WordCounter', () => {

  beforeEach(() => {
  });

  describe('METHOD NAME', () => {
    it('TEST NAME', () => {
      // Arrange
      
      // Act

      // Assert
    });
  });
});